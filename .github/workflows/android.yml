name: Trigger Jenkins Build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Trigger Jenkins Job
      run: |
        curl -X POST "https://991c-177-86-177-163.ngrok-free.app" \
        --user "<admin>:<c835285395014bada754daba2360fa6a>"
